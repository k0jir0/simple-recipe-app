<!DOCTYPE html>
<html lang="en">
<head>

    <title><%= recipe ? "Edit Recipe" : "Create Recipe" %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet"> 

    <script src="/js/recipe-form.js" defer></script>
</head>
<body>

    <div class="container mt-5">
        <h1 class="mb-4">
            <%= recipe ? "Edit Recipe" : "Create Recipe" %>
        </h1>

        <form id="recipe-form" data-id="<%= recipe ? recipe._id : '' %>">
        
            <div class="mb-3">
                <label class="form-label">Recipe Name</label>

                <input
                type="text"
                name="name"
                class="form-control"
                required
                value="<%= recipe ? recipe.name : '' %>"
                
                />
            </div>
        
            <div class="mb-3">
                <label class="form-label">Description</label>
                <textarea
                name="description"
                class="form-control"
                rows="3"
                ><%= recipe ? recipe.description : '' %></textarea>
            </div>

            <div class="mb-3">
                <label class="form-label">Image URL</label>
                <input
                type="text"
                name="image"
                class="form-control"
                value="<%= recipe ? recipe.image : '' %>"
                />
            </div>

            <div class="col-md-6 mb-3">
                <label class="form-label">Preparation Time (mins)</label>
                <input
                type="number"
                name="prepTime"
                class="form-control"
                value="<%= recipe ? recipe.prepTime : '' %>"
                />
            </div>

                <div class="col-md-6 mb-3">
                <label class="form-label">Cook Time (mins)</label>
                <input
                type="number"
                name="cookTime"
                class="form-control"
                value="<%= recipe ? recipe.cookTime : '' %>"
                />
            </div>

                        <div class="mb-3">
                <label class="form-label">Ingredients</label>
                <textarea
                name="ingredients"
                class="form-control"
                rows="5"
                ><%= recipe ? recipe.ingredients.join('\n')  : '' %></textarea>
            </div>



                                    <div class="mb-3">
                <label class="form-label">Instructions</label>
                <textarea
                name="instructions"
                class="form-control"
                rows="5"
                ><%= recipe ? recipe.instructions.join('\n')  : '' %></textarea>
            </div>



            <button type="submit" class="btn btn-dark">
                <%= recipe ? "Update Recipe" : "Create Recipe" %>
            </button>

            <a href="/" class="btn btn-secondary">
                Cancel</a>

        </form>
    </div>
</body>
</html>